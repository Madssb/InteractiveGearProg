# osrs_milestone_metadata

OSRS milestone metadata resolver package used by this repository.

## Usage

Python:

```python
from osrs_milestone_metadata import query_milestone_metadata_record, query_milestone_metadata
```

CLI:

```bash
osrs-milestone-metadata --help
```
